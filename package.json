{
  "name": "metrics-dashboard-demo",
  "private": true,
  "scripts": {
    "build": "esbuild Analyzer_Demo.js --bundle --loader:.js=jsx --outfile=app.js --jsx=transform --jsx-factory=React.createElement --jsx-fragment=React.Fragment --global-name=__app --format=iife",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "papaparse": "^5.5.3"
  }
}
