{
  "name": "metrics-dashboard-demo",
  "private": true,
  "scripts": {
    "build": "esbuild Analyzer_Demo.js --loader:.js=jsx --outfile=app.js --jsx=transform --jsx-factory=React.createElement --jsx-fragment=React.Fragment",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
